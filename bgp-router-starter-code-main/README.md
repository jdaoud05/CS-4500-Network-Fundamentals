### High-level approach and overview of tested code

Our high level approach was to first implement the route update, withdraw, and data messages. As we went through the project, we tested our code through the different configuration files and verified that each one worked one-by-one. Towards the end, we implemented the aggregation and applied the specific rules our router should follow to decide whcih entry to use through the best_route() function.

### Challenges Faced

We had trouble implementing the aggregation of the router. This was a new topic to us where there were multiple steps we had to consider when programming the aggregation. We overcame this challenge by first building a solid conceputal understanding through course slides and online resources, then translated that knowledge into code by breaking it down into several functions.